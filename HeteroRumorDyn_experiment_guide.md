# HeteroRumorDyn 个性化实验开发指导书

> **论文**: 面向异构社交生态的谣言传播动力与控制模型（徐淇 V2.6）  
> **任务类型**: 谣言传播动力学建模 + 破圈临界预警 + 时滞闭环控制  
> **目标期刊**: IEEE TKDE / Information Sciences / Applied Mathematical Modelling  
> **评审场景**: 场景 0（实验开发指导）→ 场景 3（实验审查）  
> **生成日期**: 2026-05-30  
> **评审标准**: writing-standards.md §6 + review-standards.md §Experimental Completeness Checklist（14 项）

---

## 一、论文 → 实验映射表

| 引言挑战 | 方法贡献 | 核心模块 | 验证实验 | 必须输出的图表 |
|---------|---------|---------|---------|--------------|
| C1: 欺骗性耦合下传播动能难以辨识 | 反事实干预的传播动能因果辨识 | PLM 文本编码, GAT 拓扑编码, VAE 因子分解, 反事实约束 | **主实验**: 传播规模预测误差 vs SIR/SEIR/DeepHawkes；**消融**: 去掉反事实约束后的噪声敏感性；**可解释性**: 因子可视化 | 表1（传播动力学拟合对比）, 图3（动能因子 t-SNE / 反事实前后对比） |
| C2: 极化圈层中的破圈临界预警存在滞后性 | 频谱—动态图协同的破圈临界预警 | 图传播信号, LSTM 时序, TGN 动态图, 低频能量检测, 跨社区拓扑变化 | **主实验**: 破圈预警 AUC/Recall@k 与静态图模型对比；**消融**: 去掉低频能量或跨社区检测；**时间增益**: 提前预警时间 Δt | 表2（破圈预警性能）, 图4（低频能量演化 + 破圈临界点标注）, 图5（TGN 动态图快照可视化） |
| C3: 博弈时滞下动态控制容易失准 | 破圈风险驱动的时滞闭环控制 | 时滞传播模型, 主从博弈, 事件触发脉冲控制 | **主实验**: 干预后传播规模抑制率 vs 固定策略/影响力阻断；**消融**: 去掉博弈建模（退化为单主体）或去掉事件触发（退化为定时干预）；**成本分析**: 干预成本-收益曲线 | 表3（控制效果对比）, 图6（传播曲线：无干预/固定干预/本文方法）, 图7（脉冲干预时机与强度动态调节） |

**输入输出一致性校验**:
- 问题定义输入: 多模态内容 + 用户属性 + 社交拓扑 + 时序传播日志
- 问题定义输出: 传播动能因子 + 破圈风险信号 + 干预时机/强度
- **实验必须同时验证**: (a) 传播规模预测准确性（动力学一致性）; (b) 破圈预警及时性（提前量）; (c) 控制有效性（抑制率+成本）

---

## 二、V1 → V4 渐进式开发路线图

### 阶段 V0：环境搭建与数据准备（第 1–2 周）

**目标**: 可运行的数据加载器 + 经典基线复现

```
Week 1:
  □ 选择 2–3 个公开传播数据集（见§三推荐）
  □ 完成数据预处理：文本 tokenization（BERT tokenizer）+ 用户特征编码 + 传播树/图构建
  □ 构建时序传播日志：每个时间步的转发/评论记录 → 动态图快照序列
  □ 标注破圈事件：人工或启发式标注跨社区传播的时间点（作为 C2 的 ground truth）
  □ 实现经典 SIR/SEIR 模型（作为动力学基线）

Week 2:
  □ 复现 3 个基线（DeepHawkes, CasCN, TGN）
  □ 建立统一的数据加载接口（保证所有模型接收：文本+用户+拓扑+时序日志）
  □ 确定评价指标：
      - 动力学拟合：MSE, MAE, MAPE（传播规模曲线）
      - 破圈预警：AUC, Precision@k, Recall@k, 平均提前时间 Δt
      - 控制效果：传播规模抑制率, 干预成本, 成本-收益比
  □ 建立实验记录表格（每次实验记录 seed, 超参数, 结果）
```

### 阶段 V1：骨架模型（第 3–4 周）

**目标**: 可训练的端到端传播预测模型（不含反事实/VAE/博弈/脉冲控制）

```
Week 3:
  □ 实现贡献 1 骨架：BERT 文本编码 + GAT 拓扑编码 → 拼接 → MLP 预测传播规模
  □ 实现时序编码：LSTM 处理传播日志的时序特征
  □ 训练并验证：应达到或接近 DeepHawkes / TGN 水平

Week 4:
  □ V1 消融：去掉拓扑编码（仅文本）→ 验证图结构必要性
  □ V1 消融：去掉时序编码（静态图）→ 验证动态演化必要性
  □ 调试数据流：确认输入输出与问题定义一致
```

**V1 验收标准**: MAPE ≤ 30%（传播规模预测），无明显 bug。

### 阶段 V2：核心创新模块逐次加入（第 5–8 周）

**目标**: 加入 VAE 因子分解、反事实约束、TGN 动态图、频谱分析、主从博弈、事件触发脉冲控制

```
Week 5:
  □ 实现 VAE 因子分解：将多源表征映射到低维传播动能因子（如 8–16 维）
  □ 实现反事实约束：构建反事实样本（如随机打乱用户-内容匹配），约束预测不变性
  □ V2a 消融：去掉反事实约束 → 验证噪声敏感性（在含噪声数据集上测试）
  □ V2b 消融：VAE 因子数 K={4,8,16,32} 敏感性分析

Week 6:
  □ 实现 TGN：将传播日志转化为动态图，使用 TGN 编码时序图演化
  □ 实现低频能量检测：对传播信号做傅里叶变换/小波变换，提取低频分量
  □ 实现跨社区拓扑变化检测：监测社区间边权重变化率
  □ V2c 消融：去掉低频能量 → 验证隐性积聚感知能力
  □ V2d 消融：去掉跨社区检测 → 验证破圈预警能力

Week 7:
  □ 实现时滞传播模型：在 SEIR 基础上引入时滞项 τ（平台审核延迟）
  □ 实现主从博弈：平台（leader）先决策干预强度，传播者（follower）后决策规避策略
  □ 求解博弈均衡：数值求解或近似求解（如梯度下降/迭代 best response）
  □ V2e 消融：去掉博弈（退化为单主体最优控制）→ 验证策略对抗必要性

Week 8:
  □ 实现事件触发脉冲控制：根据破圈风险信号动态触发干预，调节强度
  □ 联合训练/推理：三个模块串联（动能辨识 → 破圈预警 → 闭环控制）
  □ 端到端验证：完整 pipeline 在测试集上跑通
  □ 检查梯度流/数值稳定性：博弈求解是否收敛？脉冲控制是否震荡？
```

**V2 验收标准**: 
- C1: 相比 V1，MAPE 降低 ≥ 5%，反事实约束在噪声数据上提升 ≥ 8%
- C2: 破圈预警 AUC ≥ 0.85，平均提前时间 Δt ≥ 2 小时（或 20% 传播周期）
- C3: 传播规模抑制率 ≥ 40%（vs 无干预），且干预成本低于固定策略

### 阶段 V3：完整实验（第 9–12 周）

**目标**: 14 项实验清单全部落实

```
Week 9:
  □ 主实验：在 2–3 个数据集上跑满 5 个随机种子
  □ 统计显著性：t-test / bootstrap 95% CI
  □ 基线对比：确保覆盖经典 + SOTA（3–5 个基线）

Week 10:
  □ 消融实验：每个核心模块独立消融（见§四消融设计方案）
  □ 参数敏感性：VAE 因子数 K, 时滞 τ, 博弈迭代次数, 触发阈值, 脉冲强度上限
  □ 效率实验：训练时间, 推理时间, 显存占用（尤其 TGN 和博弈求解）

Week 11:
  □ 可视化：动能因子 t-SNE, 反事实前后对比, 低频能量演化, 破圈临界点, 动态图快照, 传播曲线对比
  □ 案例分析：展示 2–3 个成功预警案例 + 1–2 个误报/漏报案例分析
  □ 鲁棒性测试：不同噪声比例（10%/20%/30% 伪标签）下的性能变化

Week 12:
  □ 跨数据集泛化：在数据集 A 训练，数据集 B 测试（零样本 / 微调）
  □ 消融反直觉结果排查与修复
```

**V3 验收标准**: 14 项清单全部 ✓，结果与摘要声明一致。

### 阶段 V4：论文衔接与可复现性（第 13 周）

```
Week 13:
  □ 整理实验表格（主实验 + 消融 + 参数敏感 + 效率）
  □ 核对数字一致性：摘要 ↔ 引言贡献声明 ↔ 实验表格
  □ 开源准备：代码整理 + README（数据集路径, 运行命令, 依赖版本）
  □ 生成 checkpoint：保存最优模型权重 + 预处理后数据供审稿人复现
```

---

## 三、推荐基线、数据集与复现命令

### 3.1 推荐数据集（≥2 个）

| 数据集 | 来源 | 规模 | 模态 | 是否可直接使用 | 适配说明 |
|--------|------|------|------|--------------|---------|
| **Weibo Rumor Dataset** | Ma et al., 2016 | ~4,600 条 + 传播树 | 文本 + 传播结构 + 时间戳 | ⚠️ 需补充用户属性 | 最常用中文数据集，需补充用户粉丝数/认证状态等属性 |
| **Twitter15/16** | Ma et al., 2017 | ~2,000 条 | 文本 + 传播结构 + 时间戳 | ⚠️ 同上 | 国际数据集，增强泛化说服力 |
| **Weibo COVID-19** | 自建/公开 | 大规模 | 文本 + 传播结构 + 时间戳 | ✅ 可构建 | 若需大规模实验，可收集 COVID-19 相关谣言传播数据 |
| **PHEME** | Zubiaga et al., 2016 | ~5,800 条 | 文本 + 传播结构 + 时间戳 | ⚠️ 事件标注 | 含 5 个突发事件的谣言传播，适合跨事件泛化测试 |

> **TKDE 要求**: 需详细说明数据收集时间、预处理流程、社区检测方法（如 Louvain/Leiden）、破圈事件标注方式。

### 3.2 推荐基线（3–5 个，覆盖经典 + SOTA）

| 基线 | 类型 | 代表论文 | 复现来源 | 必须对比的维度 |
|------|------|---------|---------|--------------|
| **SIR / SEIR** | 宏观动力学经典 | Kermack 1927 | 自实现 | 验证"微观特征+深度学习"对宏观模型的超越 |
| **DeepHawkes** | 深度传播建模 SOTA | Cao et al., CIKM 2017 | GitHub 开源 | 验证"时序依赖建模"基线 |
| **CasCN / CasFlow** | 级联预测 SOTA | AAAI 2019/2020 | 论文开源代码 | 验证"传播规模预测"SOTA |
| **TGN** | 动态图网络 SOTA | Rossi et al., ICLR 2020 | PyG 官方实现 | 验证"动态图演化"基线 |
| **Inf-VAE / GNN-SD** | 传播动力学 + 深度学习 | AAAI 2024 | 论文开源代码 | 验证"动力学一致性"最新方法 |
| **Fixed Intervention** | 控制策略基线 | 自实现 | 固定时间/固定强度干预 | 验证"动态控制"优于静态策略 |
| **Influence Blocking** | 影响力阻断经典 | Budak et al., 2011 | 自实现 | 验证"博弈+脉冲"优于传统阻断 |

**基线适配命令模板**:

```bash
# 1. 宏观动力学基线（SEIR）
python baseline_seir.py --dataset weibo --beta 0.5 --gamma 0.1 --delay 2.0 --epochs 100

# 2. 深度传播基线（DeepHawkes）
python baseline_deephawkes.py --dataset weibo --emb_dim 128 --rnn_dim 64 --epochs 50

# 3. 动态图基线（TGN）
python baseline_tgn.py --dataset weibo --node_dim 128 --time_dim 32 --memory_dim 128 --epochs 50

# 4. 本文模型（HeteroRumorDyn）
python train_heterorumor.py --dataset weibo \
  --plm bert-base-chinese --gat_heads 8 --vae_latent_dim 16 \
  --tgn_memory_dim 128 --lstm_hidden 64 \
  --delay_tau 2.0 --game_iter 5 --trigger_threshold 0.7 --pulse_max 0.5 \
  --lambda_cf 0.3 --lambda_kl 0.1 --epochs 50 --seed 42
```

### 3.3 关键超参数初始建议

| 超参数 | 初始值 | 搜索范围 | 敏感性 |
|--------|--------|---------|--------|
| PLM 编码器 | bert-base-chinese | {bert-base-chinese, roberta-wwm} | 中 |
| GAT 头数 | 8 | {4, 8, 16} | 中 |
| GAT 层数 | 2 | {1, 2, 3} | 中 |
| VAE 隐变量维度 K | 16 | {4, 8, 16, 32} | **高**（影响可解释性） |
| VAE KL 权重 λ_KL | 0.1 | {0.01, 0.05, 0.1, 0.5} | 高 |
| 反事实约束权重 λ_cf | 0.3 | {0.1, 0.3, 0.5, 1.0} | **高** |
| TGN memory dim | 128 | {64, 128, 256} | 中 |
| LSTM 隐藏维度 | 64 | {32, 64, 128} | 低 |
| 低频能量阈值 | 0.2 | {0.1, 0.2, 0.3, 0.5} | **高**（影响预警灵敏度） |
| 时滞 τ | 2.0h | {0.5, 1.0, 2.0, 4.0} | **高**（需与数据集匹配） |
| 博弈迭代次数 | 5 | {1, 3, 5, 10} | 中（影响收敛速度） |
| 事件触发阈值 | 0.7 | {0.5, 0.6, 0.7, 0.8, 0.9} | **高**（影响干预时机） |
| 脉冲强度上限 | 0.5 | {0.3, 0.5, 0.7, 1.0} | 高 |
| 学习率 | 1e-4 | {5e-5, 1e-4, 5e-4} | 高 |
| Dropout | 0.3 | {0.1, 0.3, 0.5} | 中 |

---

## 四、消融实验设计方案（含因果命题）

### 4.1 消融实验总表

| 消融变体 | 因果命题 | 操作方式 | 预期结果 | 验证挑战 |
|---------|---------|---------|---------|---------|
| w/o Counterfactual Constraint | 反事实约束是抑制欺骗性噪声、提升传播动能辨识**稳定性**的必要条件 | 删除反事实损失，仅使用 VAE 重构损失 | 含噪声数据上 MAPE ↑ ≥ 8%，因子可解释性 ↓ | C1 |
| w/o VAE Factorization | VAE 因子分解是学习低维可解释传播动能的**必要条件** | 删除 VAE，直接拼接多源表征预测 | MAPE ↑ ≥ 5%，且无法可视化因子语义 | C1 |
| w/o Dynamic Graph (TGN) | 动态图网络是刻画传播拓扑时序演化的**必要条件** | 删除 TGN，使用静态图 + LSTM | 破圈预警 AUC ↓ ≥ 5%，提前时间 Δt ↓ | C2 |
| w/o Low-Frequency Energy | 低频能量检测是识别隐性积聚阶段的**必要条件** | 删除频谱分析，仅使用拓扑变化率 | 破圈前隐性积聚阶段 Recall ↓ ≥ 10% | C2 |
| w/o Cross-Community Topology | 跨社区拓扑变化检测是识别破圈临界信号的**必要条件** | 删除社区间边监测 | 破圈突变时 Precision ↓ ≥ 8% | C2 |
| w/o Leader-Follower Game | 主从博弈是刻画平台-传播者策略对抗的**必要条件** | 退化为单主体最优控制（平台单方面决策） | 传播者规避率上升，最终抑制率 ↓ ≥ 10% | C3 |
| w/o Event-Triggered Pulse | 事件触发机制是实现自适应干预时机选择的**必要条件** | 退化为固定周期/固定强度干预 | 干预成本 ↑ ≥ 30%，或同等成本下抑制率 ↓ ≥ 15% | C3 |
| w/o Time Delay | 时滞项是刻画平台审核延迟影响的**必要条件** | 设置 τ = 0 | 干预时机与实际生效错位，控制效果 ↓ | C3 |

### 4.2 因果命题验证规范

每个消融实验必须回答以下三个问题：

1. **必要性**: 删除该模块后，目标指标是否显著恶化？（若变化 < 3%，则该模块非必要）
2. **充分性**: 仅保留该模块（其他模块退化为最简），是否仍优于对应基线？
3. **不可替代性**: 该模块能否被简单替代（如 TGN → 静态图 + 时序拼接）而不损失性能？

**反直觉结果处理**:
- 若消融后性能上升 → 检查模块是否与损失函数冲突 / 是否超参数未调优 / 是否引入数值不稳定（如博弈振荡）
- 若消融后性能不变 → 检查模块是否真正参与计算（梯度是否流动 / 是否被短路）

---

## 五、14 项实验清单自查表

| 编号 | 检查项 | 标准 | 你的状态 | 评分 | 优先级 |
|------|--------|------|---------|------|--------|
| E1 | 数据集 ≥2 个 | 至少 2 个公开数据集 + 描述完整（来源、规模、社区检测方法） | ⬜ 未开始 | — | P0 |
| E2 | 基线 3–5 个 | 覆盖经典（SEIR/DeepHawkes）+ SOTA（TGN/Inf-VAE）+ 控制基线（固定策略） | ⬜ 未开始 | — | P0 |
| E3 | 评价指标匹配 | 动力学拟合（MSE/MAPE）+ 预警（AUC/Δt）+ 控制（抑制率/成本） | ⬜ 未开始 | — | P0 |
| E4 | 统计显著性 | 5 个随机种子 + t-test / bootstrap 95% CI | ⬜ 未开始 | — | P1 |
| E5 | 主实验最优/次优加粗 | 表格中最佳结果加粗，次优下划线 | ⬜ 未开始 | — | P1 |
| E6 | Ablation 每个核心模块 | VAE, 反事实, TGN, 频谱, 跨社区, 博弈, 脉冲控制均有消融 | ⬜ 未开始 | — | P0 |
| E7 | 参数敏感性 | 至少 2 个关键超参数（如 K, τ, 触发阈值）有分析图 | ⬜ 未开始 | — | P1 |
| E8 | 效率实验 | 训练时间 / 推理时间 / 显存 vs 最强基线对比；博弈求解收敛时间 | ⬜ 未开始 | — | P2 |
| E9 | 可视化 | 动能因子 t-SNE, 反事实对比, 低频能量演化, 破圈临界点, 传播曲线, 脉冲干预轨迹 | ⬜ 未开始 | — | P1 |
| E10 | 案例分析 | 2–3 个成功预警/控制案例 + 1–2 个失败案例分析（附传播树可视化） | ⬜ 未开始 | — | P2 |
| E11 | 结果一致性 | 摘要数字 ↔ 引言贡献声明 ↔ 实验表格 三者一致 | ⬜ 未开始 | — | P0 |
| E12 | 早期检测/预警声明 | 若声称"提前预警"，需报告平均提前时间 Δt 和不同提前量下的 Recall | ⬜ 未开始 | — | P1 |
| E13 | 跨数据集泛化 | 在数据集 A 训练，数据集 B 测试（零样本 / 微调）；跨事件泛化（PHEME） | ⬜ 未开始 | — | P1 |
| E14 | 可复现性 | 框架（PyTorch 版本）、硬件（GPU 型号）、超参数表格、社区检测工具版本完整 | ⬜ 未开始 | — | P1 |

**当前状态**: 14 项中 **0 项完成**。建议按 V1→V4 路线图逐步落实。

---

## 六、动力学建模类论文的特殊注意事项

### 6.1 动力学一致性验证（区别于纯预测任务）

TKDE / 应用数学类期刊对传播动力学模型有额外要求：

| 验证项 | 方法 | 最低要求 |
|--------|------|---------|
| **基本再生数 R₀** | 计算模型对应的 R₀，分析稳定性条件 | 需给出 R₀ 表达式，证明无病平衡点稳定性 |
| **敏感性分析** | 对关键参数（β, γ, τ）做偏导数/蒙特卡洛分析 | 至少 3 个参数的单因素敏感性 |
| **相图分析** | 绘制 S-I-R 相轨迹，验证与真实数据趋势一致 | 至少 1 个数据集的相图对比 |
| **长期行为** | 模拟 t → ∞ 时系统是否收敛到稳态 | 展示 500+ 时间步的收敛曲线 |

### 6.2 控制效果评估规范

| 评估维度 | 指标 | 说明 |
|---------|------|------|
| **有效性** | 传播规模抑制率 = (无干预规模 − 干预后规模) / 无干预规模 | 越高越好，通常 ≥ 30% 才有说服力 |
| **及时性** | 干预响应时间 = 风险信号触发 → 干预生效的时间差 | 需与 τ 匹配 |
| **经济性** | 干预成本 = Σ(脉冲强度 × 持续时间) 或 阻断节点数 | 同等抑制率下成本越低越好 |
| **鲁棒性** | 不同传播者规避策略（保守/激进/自适应）下的抑制率变化 | 至少 2 种规避策略 |

### 6.3 博弈模型求解验证

| 验证项 | 方法 |
|--------|------|
| 纳什均衡存在性 | 证明收益函数的凹性/拟凹性，或使用数值验证 |
| 收敛性 | 记录博弈迭代过程中的收益变化，证明收敛 |
| 策略合理性 | 展示平台最优策略随破圈风险的变化趋势（应单调不减） |

---

## 七、引用学生手册

本指导书与以下通用手册配套使用：

| 手册 | 路径 | 用途 |
|------|------|------|
| **00-quickstart.md** | `docs/templates/student-handbook/00-quickstart.md` | 30 秒导航：出问题先看哪本手册的哪一章 |
| **01-development-guide.md** | `docs/templates/student-handbook/01-development-guide.md` | V1→V4 渐进式开发通用指南、代码骨架、14 项清单落实 |
| **02-debug-manual.md** | `docs/templates/student-handbook/02-debug-manual.md` | DataAgent / ModelAgent / TrainAgent / AblateAgent 诊断清单 |

### 使用顺序

```
开始实验
  ↓
[阶段 V0–V1] 参考 01-development-guide.md §5–§6（环境搭建 + V1 骨架）
  ↓ 遇到问题（传播曲线拟合差 / Loss 震荡 / 博弈不收敛 / 消融反直觉）
[诊断] 打开 02-debug-manual.md，匹配症状 → Agent → 检查清单 → 修复方案
  ↓
[阶段 V2–V3] 参考本指导书 §二（V2–V3 路线图）+ §四（消融设计）+ §六（动力学特殊要求）
  ↓
[阶段 V4] 参考 01-development-guide.md §9（结果整理 → 论文衔接）
  ↓
实验完成 → 进入场景 2/3/4 评审
```

---

*本指导书由 Agent 2（Methodology Expert）+ Agent 5（Journal Matcher）基于徐淇 V2.6 论文定制生成。*  
*通用开发规范参见 `docs/templates/student-handbook/01-development-guide.md`。*  
*动力学建模特殊要求参见 `writing-standards.md` §6.8（若适用）。*
