# 实验调试与自检手册（学生版）

> **文档定位**：当你在实验过程中遇到指标异常、Loss不下降、消融结果反直觉等问题时，按本手册逐步排查。
>
> **使用前提**：已阅读《实验开发指导手册》并完成至少 V1 骨架搭建。
>
> **版本**：v1.0

---

## 0. 30秒导航：我现在该翻开哪一页？

```
你现在处于什么状态？
    │
    ├─→ 【还没开始写代码】或【准备进入下一阶段】
    │     └─→ 请切换到《实验开发指导手册》
    │           打开它的 §0「按阶段速查表」
    │
    └─→ 【正在跑实验但卡住了/报错了/指标不对】
          └─→ 你正在读正确的文档（本手册）
                见下方「按症状速查表」
```

### 按症状速查表

| 你的症状 | 首选智能体 | 翻到本手册的... | 大概需要多久 |
|---------|-----------|----------------|------------|
| 训练直接报错（RuntimeError / size mismatch） | **ModelAgent** | §4.2 检查清单 M1-M10 | 10-30 min |
| 指标比基线低 > 5% 或接近随机 | **DataAgent** | §3.2 检查清单 D1-D10 | 20-60 min |
| Loss 完全不下降 | **TrainAgent** → ModelAgent | §5.2 T1-T3 → §4.2 M6 | 20-30 min |
| Loss 震荡 / 爆炸 / 出现 NaN | **TrainAgent** | §5.2 T1-T8 | 15-30 min |
| 训练Loss↓但验证Loss↑（过拟合） | **TrainAgent** | §5.2 T6 | 15 min |
| 去掉某模块后性能反而提升 | **AblateAgent** | §6.3 消融异常处理 | 30-60 min |
| 消融变体系统崩溃 | **ModelAgent** | §4.2 M10 接口检查 | 15 min |
| 基线复现不了（误差 > 5%） | **DataAgent** | §3.2 D1-D10 | 30-60 min |
| GPU OOM（显存溢出） | **TrainAgent** | §5.2 T4 | 10 min |
| 多个问题同时出现，不知道先修哪个 | **元调度器** | §7 标准诊断流程 | 5 min |

**排查铁律（必须遵守）**：

```
先查数据（DataAgent §3）→ 再查模型（ModelAgent §4）→ 再查训练（TrainAgent §5）→ 最后查消融（AblateAgent §6）
```

> **更完整的双文档导航** → 参见《实验双文档快速入门》（1页纸）

---

## 目录

1. [调试智能体总览](#一调试智能体总览)
2. [问题→智能体快速匹配](#二问题智能体快速匹配)
3. [DataAgent：数据诊断](#三dataagent数据诊断)
4. [ModelAgent：模型诊断](#四modelagent模型诊断)
5. [TrainAgent：训练诊断](#五trainagent训练诊断)
6. [AblateAgent：消融诊断](#六ablateagent消融诊断)
7. [元诊断调度器使用流程](#七元诊断调度器使用流程)
8. [自检清单汇总](#八自检清单汇总)
9. [常见问题速查FAQ](#九常见问题速查faq)

---

## 一、调试智能体总览

本手册基于4个诊断智能体 + 1个调度器的协作体系。你不用理解背后的系统架构，只需要知道：**遇到什么问题 → 找哪个智能体 → 按什么顺序检查**。

| 智能体 | 解决哪类问题 | 典型症状 | 你需要提供的输入 |
|--------|-------------|---------|---------------|
| **DataAgent** | 数据加载、划分、特征问题 | 指标异常低、数据报错、复现不了基线 | 数据加载代码、数据划分逻辑、样本示例 |
| **ModelAgent** | 模型结构、维度、梯度问题 | size mismatch、loss不下降、输出异常 | 模型定义代码、forward函数、各层输出shape |
| **TrainAgent** | 训练动态、超参数问题 | loss震荡、过拟合、收敛慢 | 训练日志、loss曲线、当前超参数配置 |
| **AblateAgent** | 消融实验设计、结果解释 | 消融反直觉、缺少对照组、无法解释 | 消融变体列表、各变体指标、论文贡献描述 |

> **元调度器规则**：先查数据 → 再查模型 → 再查训练 → 最后查消融。低层问题会掩盖高层问题，必须先排除。

---

## 二、问题→智能体快速匹配

### 2.1 症状速查表

| 症状 | 优先级 | 首选智能体 | 次选智能体 |
|------|--------|-----------|-----------|
| 训练直接报错（RuntimeError） | P0 | ModelAgent | DataAgent |
| 指标比基线低 > 5% | P0 | DataAgent | ModelAgent |
| Loss 完全不下降 | P0 | TrainAgent | ModelAgent |
| Loss 震荡/爆炸 | P0 | TrainAgent | ModelAgent |
| 训练Loss↓但验证Loss↑ | P1 | TrainAgent | — |
| 消融 w/o X 性能反而提升 | P1 | AblateAgent | ModelAgent |
| 消融结果无法解释 | P1 | AblateAgent | — |
| 复现基线误差 > 5% | P0 | DataAgent | — |
| GPU OOM | P1 | TrainAgent | ModelAgent |
| 训练速度异常慢 | P2 | TrainAgent | — |

### 2.2 诊断流程图（简化版）

```
实验出问题
    │
    ├─→ 报错/崩溃？ ──→ ModelAgent §4 ──→ DataAgent §3
    │
    ├─→ 指标远低于基线？ ──→ DataAgent §3 ──→ ModelAgent §4
    │
    ├─→ Loss异常？ ──→ TrainAgent §5
    │
    └─→ 消融异常？ ──→ AblateAgent §6
```

---

## 三、DataAgent：数据诊断

### 3.1 触发条件
- 基线复现误差 > 2%
- 指标异常低（随机水平或接近0）
- 数据加载报错
- 怀疑自己数据划分有问题

### 3.2 检查清单（逐项勾选）

```markdown
□ D1. 数据格式是否与基线代码一致？
      检查点：cascades.txt 格式、edges.txt 格式、分隔符
      参考：《实验开发指导手册》§4.2

□ D2. 训练/测试划分是否按时间切分？
      检查点：若按随机切分 → 可能存在数据泄漏
      修复：改为按时间窗口切分

□ D3. 特征维度是否与模型输入一致？
      检查点：print(data.shape) 对比模型 __init__ 中的输入定义
      常见错误：少了特征维度、多了batch维度

□ D4. 标签编码是否与基线一致？
      检查点：分类任务中类别ID是否从0开始连续编码
      常见错误：类别ID不连续导致 Embedding 索引越界

□ D5. 是否存在数据泄漏？
      检查点：测试集样本是否出现在训练集中？
      快速检测：取测试集前10个ID，在训练集中查找

□ D6. PAD/MASK处理是否正确？
      检查点：损失函数是否忽略了 PAD 位置？
      常见错误：PAD参与loss计算导致数值异常

□ D7. 数据归一化/标准化是否与基线一致？
      检查点：是否使用了相同的 scaler？

□ D8. 数据加载器是否有shuffle？
      检查点：训练集需要shuffle，验证/测试集不需要

□ D9. 样本数是否足够？
      检查点：训练集样本数 / 模型参数量 > 10

□ D10. 特征是否包含未来信息？
      检查点：时序任务中，特征构造是否用了未来数据？
```

### 3.3 常见数据问题修复方案

| 问题 | 诊断方法 | 修复方案 |
|------|---------|---------|
| 数据泄漏 | 测试ID出现在训练集 | 按时间切分，确保训练时间 < 测试时间 |
| 格式不匹配 | 对比基线数据loader | 统一为基线的数据格式 |
| 特征维度错 | print shape对比 | 调整数据预处理或模型输入层 |
| PAD未忽略 | 检查loss计算逻辑 | 增加 mask 过滤 PAD |

---

## 四、ModelAgent：模型诊断

### 4.1 触发条件
- RuntimeError（size mismatch、维度不匹配）
- forward输出异常（全0、全1、NaN、Inf）
- 某层梯度为0或爆炸
- 模块接口怀疑有问题

### 4.2 检查清单

```markdown
□ M1. 问题定义中的输入输出是否与forward一致？
      检查点：对比《实验开发指导手册》中的接口规范注释
      参考：writing-standards §5.2 输入输出一致性

□ M2. 各层输出维度是否连续匹配？
      诊断代码：
      ```python
      def check_dimensions(model, sample_input):
          for name, module in model.named_modules():
              # 打印每层输出shape
      ```

□ M3. Embedding层输入是否越界？
      检查点：input.max() < vocab_size / num_users
      常见错误：PAD索引=vocab_size导致越界

□ M4. 激活函数放置是否正确？
      检查点：最后一层是否有不必要的激活（如softmax+sigmoid）

□ M5. 损失函数与任务类型是否匹配？
      检查点：
      - 多分类 → CrossEntropyLoss
      - 二分类 → BCEWithLogitsLoss
      - 回归 → MSELoss

□ M6. 各模块梯度是否正常？
      诊断代码：
      ```python
      for name, param in model.named_parameters():
          if param.grad is not None:
              print(f"{name}: grad_norm={param.grad.norm()}")
      ```
      判断标准：
      - grad_norm < 1e-6 → 梯度消失
      - grad_norm > 100 → 梯度爆炸
      - grad_norm = 0 → 该层未参与反向传播

□ M7. BatchNorm/LayerNorm位置是否正确？
      检查点：
      - BN通常在卷积/全连接后、激活前
      - 序列模型中常用LayerNorm替代BatchNorm

□ M8.  dropout是否在训练时启用、推理时关闭？
      检查点：model.train() / model.eval() 是否正确切换

□ M9.  模块-挑战匹配度自检
      检查点：删除该模块后，对应的挑战是否真的无法解决？
      参考：writing-standards §4.2

□ M10. 模块间接口可行性自检
      检查点：
      - 形式接口：输出维度 = 输入维度？
      - 语义接口：输出的物理含义与输入需求一致？
      - 梯度接口：连接操作是否可导？
      参考：writing-standards §4.3
```

### 4.3 常见模型问题修复方案

| 问题 | 诊断方法 | 修复方案 |
|------|---------|---------|
| size mismatch | 打印各层输入输出shape | 增加投影层（Linear）或调整维度 |
| 梯度消失 | 打印grad_norm | 换激活函数（ReLU→LeakyReLU）、加残差连接 |
| 梯度爆炸 | 打印grad_norm | 梯度裁剪（clip_grad_norm_）、降低学习率 |
| 输出NaN | 检查是否有除零/对数(0) | 加epsilon（1e-8）、检查softmax输入 |
| Embedding越界 | print(input.max()) | 检查数据编码，确保索引在合法范围 |

---

## 五、TrainAgent：训练诊断

### 5.1 触发条件
- Loss不下降或下降极慢
- Loss震荡、爆炸
- 训练Loss↓但验证Loss↑（过拟合）
- 收敛速度异常慢
- GPU OOM

### 5.2 检查清单

```markdown
□ T1. 学习率是否合适？
      快速实验：尝试 [1e-4, 1e-3, 1e-2, 1e-1]
      判断标准：
      - lr太大 → loss震荡/爆炸
      - lr太小 → loss几乎不下降

□ T2. 优化器选择是否合适？
      推荐：
      - 通用场景：Adam / AdamW
      - 需要动量：SGD + momentum
      - Transformer：AdamW + warm-up

□ T3. 是否使用学习率调度？
      推荐：StepLR / CosineAnnealingLR / ReduceLROnPlateau
      常见错误：固定学习率导致后期震荡

□ T4. batch_size是否合适？
      推荐：128, 256, 512, 1024, 2048 中尝试
      判断标准：
      - batch太大 → OOM、泛化差
      - batch太小 → 震荡、收敛慢

□ T5. 权重衰减（weight_decay）是否设置？
      推荐：1e-5 ~ 1e-3
      作用：防止过拟合

□ T6. 是否过拟合？
      诊断：train_loss ↓↓ 但 val_loss ↑↑
      修复：
      - 增加 dropout
      - 增加 weight_decay
      - 早停（early stopping）
      - 数据增强

□ T7. 是否欠拟合？
      诊断：train_loss 和 val_loss 都高且接近
      修复：
      - 增加模型容量（层数/维度）
      - 减少正则化
      - 增加训练epoch

□ T8. 损失函数权重是否平衡？
      诊断：多任务学习中某个loss占主导
      修复：调整各loss的权重系数

□ T9. 随机种子是否固定？
      检查点：torch.manual_seed(42)
      目的：保证实验可复现

□ T10. 是否使用混合精度训练？
      适用：显存不足时
      注意：某些模型对fp16敏感，可能出现NaN
```

### 5.3 训练动态诊断流程

```
观察Loss曲线
    │
    ├─→ Loss完全不下降？
    │     ├─→ 检查学习率（尝试增大10倍）
    │     ├─→ 检查梯度（是否梯度消失）
    │     └─→ 检查数据（标签是否正确）
    │
    ├─→ Loss震荡？
    │     ├─→ 降低学习率
    │     ├─→ 增大batch_size
    │     └─→ 增加学习率warm-up
    │
    ├─→ Loss爆炸？
    │     ├─→ 梯度裁剪
    │     ├─→ 降低学习率
    │     └─→ 检查是否有数值不稳定操作
    │
    └─→ 训练↓但验证↑？
          ├─→ 增加正则化
          ├─→ 早停
          └─→ 简化模型
```

---

## 六、AblateAgent：消融诊断

### 6.1 触发条件
- 消融结果与预期相反（去掉某模块性能反而提升）
- 消融实验缺少对照组
- 无法解释消融结果
- 消融变体数量不足

### 6.2 检查清单

```markdown
□ A1. 每个消融变体是否有独立语义？
      检查点：去掉的模块对应一个明确的机制/挑战
      错误示例："去掉dropout"（无独立语义）
      正确示例："去掉信息瓶颈约束"（验证压缩表示的必要性）
      参考：writing-standards §4.5

□ A2. 消融变体是否可操作？
      检查点：去掉该模块后，系统是否还能运行？
      常见错误：模块输出未定义，去掉后维度断裂

□ A3. 消融结果是否映射到贡献/挑战？
      检查点：每个消融变体对应论文中的哪个贡献？
      参考：《实验开发指导手册》§5.2 消融设计表

□ A4. 消融下降幅度是否合理？
      判断标准：
      - 核心模块：去掉后下降 2-5%（正常）
      - 辅助模块：去掉后下降 0-2%（正常）
      - 去掉后反而提升 → 模块有问题（冗余/冲突）

□ A5. 是否包含完整的消融层次？
      推荐层次：
      - 模块级消融（去掉整个模块）
      - 机制级消融（替换核心机制）
      - 参数级消融（关键超参数敏感度）

□ A6. 消融表格是否符合规范？
      检查点：
      - 包含完整模型作为上界
      - 包含最强基线作为参考
      - 标注最佳/次佳结果

□ A7. 是否有替代方案消融？
      期刊论文推荐：用另一种方法替代本模块，验证方法选择的合理性
      示例：用GCN替换GAT，对比效果

□ A8. 消融实验是否通过统计显著性检验？
      检查点：5个随机种子取平均 ± 标准差
```

### 6.3 消融异常处理指南

| 异常现象 | 可能原因 | 修复方案 |
|---------|---------|---------|
| w/o X 性能↑ | X与Y冗余；X的超参数过强 | 检查模块耦合度；重新调参 |
| w/o X 性能不变 | X作用微弱；被其他模块覆盖 | 考虑移除X或增强X的独立性 |
| w/o X 系统崩溃 | X输出未定义；接口断裂 | 补全接口定义（参见ModelAgent §4） |
| 消融幅度过大 | X是核心模块但实现有问题 | 检查X是否正确实现 |

---

## 七、元诊断调度器使用流程

### 7.1 标准诊断流程（必须按顺序执行）

```markdown
## Step 1: 收集证据（5分钟）
- [ ] 复制当前报错信息（如有）
- [ ] 截取当前训练日志（最近20行）
- [ ] 记录当前超参数配置
- [ ] 记录当前指标数值 vs 基线指标

## Step 2: 按优先级诊断（P0 → P1 → P2）

### P0 检查（必须先排除）
- [ ] DataAgent §3.2 全部勾选？
      → 任一未通过：先修复数据问题，停止后续检查
- [ ] ModelAgent §4.2 M1-M6 勾选？
      → 任一未通过：先修复模型问题，停止后续检查
- [ ] TrainAgent §5.2 T1-T3 勾选？
      → 任一未通过：先修复训练配置

### P1 检查（P0全部通过后再做）
- [ ] TrainAgent §5.2 T4-T8（过拟合/欠拟合诊断）
- [ ] AblateAgent §6.2（如正在做消融实验）

### P2 检查（优化项）
- [ ] TrainAgent §5.2 T9-T10（复现性/效率）
```

### 7.2 何时求助老师/助教

以下情况，调试智能体可能无法解决，需要人工介入：

1. **按本手册排查后问题仍未解决**（超过2小时）
2. **消融结果完全颠覆论文假设**（可能需要修改论文贡献声明）
3. **需要修改论文Introduction中的挑战/贡献描述**
4. **代码库本身有bug**（非你写的代码报错）
5. **需要新增数据集或基线**（超出当前实验范围）

---

## 八、自检清单汇总

### 8.1 每日实验前自检（2分钟）

```markdown
□ 固定随机种子
□ 确认train/eval模式切换正确
□ 确认学习率、batch_size与计划一致
□ 确认保存路径不会覆盖已有结果
```

### 8.2 每周实验后自检（10分钟）

```markdown
□ 基线指标复现误差 < 2%
□ 训练日志完整保存
□ 至少完成一个消融变体
□ 超参数配置已记录
□ 实验结果已填入论文映射表
```

### 8.3 投稿前实验自查（对应 Agent 2 + Agent 6）

| # | 检查项 | 状态 | 未通过则 |
|---|--------|------|---------|
| E1 | 数据集 ≥2 个 | ☐ | 补充数据集 |
| E2 | 数据集统计表已填 | ☐ | 写脚本统计 |
| E3 | 基线 3–5 个 | ☐ | 补充基线 |
| E4 | 近3年SOTA已覆盖 | ☐ | 查知识库补基线 |
| E5 | 指标与任务匹配 | ☐ | 检查指标定义 |
| E6 | 5个种子 + 标准差 | ☐ | 跑多次实验 |
| E7 | t-test / bootstrap | ☐ | 写统计检验脚本 |
| E8 | 消融 ≥4 项且有语义 | ☐ | 补充消融 |
| E9 | 参数敏感性 ≥2个参数 | ☐ | 补充实验 |
| E10| AUC/PR曲线（如适用） | ☐ | 画图 |
| E11| early detection验证（如声称） | ☐ | 补充比例实验 |
| — | 模块-挑战匹配度 ≥ B级 | ☐ | 重构模块 |
| — | 接口可行性无断裂 | ☐ | 修复维度/语义 |

---

## 九、常见问题速查FAQ

### Q1: 按照手册检查完还是找不到问题？
**A**: 先休息15分钟，然后重新从DataAgent Step 1开始。80%的问题是因为检查时跳过了某一步。如果仍找不到，带着检查清单的结果找老师。

### Q2: 多个问题同时出现，先修哪个？
**A**: 严格按照元调度器顺序：数据 → 模型 → 训练 → 消融。不要同时改多处，否则无法定位根因。

### Q3: 智能体建议的修复方案有风险？
**A**: 任何修改前先git commit。修复后只运行5个epoch快速验证，不要直接跑完整实验。

### Q4: 消融实验需要跑多少次？
**A**: 每个变体至少5个随机种子取平均。如果某个变体波动很大（std > mean的10%），增加到10个种子。

### Q5: 实验结果与论文假设矛盾怎么办？
**A**: 先重复检查3遍（数据/模型/训练），确认不是实现bug。如果确实矛盾，记录详细结果，与老师讨论是否需要调整论文贡献声明。

---

> **记住**：调试是实验的常态。90%的时间花在调试上，这是正常的。按手册系统排查，不要凭直觉乱改。

---

*维护者：项目团队*  
*版本：v1.0*  
*配套文档：《实验开发指导手册》*
